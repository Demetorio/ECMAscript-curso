const countries = {MX: 'mexico', CL: 'chile', AR:'argentina', PE:'peru'}

console.log(Object.entries(countries)); //-->Devuelve un array con arrays individuales de la keys y su values
console.log(Object.keys(countries)); //-->Devuelve un array solo con las keys o atributos del objeto
console.log(Object.values(countries)); //-->Devuelve un array con los values de las keys del objeto
